# This file is generated and overwritten by `flux bootstrap github`.
# The content below reflects what bootstrap will produce for this repo.
# Do not edit manually — commit changes via flux bootstrap or the Flux CLI.
---
apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: flux-system
  namespace: flux-system
spec:
  interval: 1m0s
  ref:
    branch: main
  secretRef:
    name: flux-system  # deploy key created by flux bootstrap
  url: ssh://git@github.com/bizottodbt/mbhome.git
---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: flux-system
  namespace: flux-system
spec:
  interval: 10m0s
  path: ./kubernetes/infra-cluster/flux-system
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
