# gotk-components.yaml is generated by `flux bootstrap`.
# Run the following to generate it locally (for reference only):
#   flux install --export > kubernetes/infra-cluster/flux-system/gotk-components.yaml
#
# Do NOT commit a manually-crafted version of this file.
# flux bootstrap will create and manage it automatically.
