---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: immichframe
  namespace: immichframe
  labels:
    app.kubernetes.io/name: immichframe
spec:
  maxUnavailable: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: immichframe
