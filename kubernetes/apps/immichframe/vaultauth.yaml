---
apiVersion: secrets.hashicorp.com/v1beta1
kind: VaultAuth
metadata:
  name: default
  namespace: immichframe
  labels:
    app.kubernetes.io/name: immichframe
spec:
  vaultConnectionRef: vault-secrets-operator/default
  method: kubernetes
  mount: kubernetes
  kubernetes:
    role: app-immichframe
    serviceAccount: vault-sync
    audiences:
      - vault
