---
# Talos-specific Cilium values.
#
# Talos already mounts cgroupv2 and bpffs, and Kubernetes workloads cannot load
# kernel modules. Keep SYS_MODULE out of the default Cilium capability set.
ipam:
  mode: kubernetes

kubeProxyReplacement: true
k8sServiceHost: localhost
k8sServicePort: 7445

gatewayAPI:
  enabled: true
  gatewayClass:
    create: "true"

hubble:
  enabled: true
  relay:
    enabled: true
    podDisruptionBudget:
      enabled: true
      maxUnavailable: 1
    topologySpreadConstraints:
      - maxSkew: 1
        topologyKey: kubernetes.io/hostname
        whenUnsatisfiable: ScheduleAnyway
        labelSelector:
          matchLabels:
            app.kubernetes.io/name: hubble-relay
            app.kubernetes.io/part-of: cilium
  ui:
    enabled: true
    podDisruptionBudget:
      enabled: true
      maxUnavailable: 1
    topologySpreadConstraints:
      - maxSkew: 1
        topologyKey: kubernetes.io/hostname
        whenUnsatisfiable: ScheduleAnyway
        labelSelector:
          matchLabels:
            app.kubernetes.io/name: hubble-ui
            app.kubernetes.io/part-of: cilium

l2announcements:
  enabled: true

k8sClientRateLimit:
  qps: 20
  burst: 40

cgroup:
  autoMount:
    enabled: false
  hostRoot: /sys/fs/cgroup

securityContext:
  capabilities:
    ciliumAgent:
      - CHOWN
      - KILL
      - NET_ADMIN
      - NET_RAW
      - IPC_LOCK
      - SYS_ADMIN
      - SYS_RESOURCE
      - DAC_OVERRIDE
      - FOWNER
      - SETGID
      - SETUID
    cleanCiliumState:
      - NET_ADMIN
      - SYS_ADMIN
      - SYS_RESOURCE

operator:
  replicas: 2
  extraEnv:
    # Kubernetes 1.35+ enables client-go WatchListClient by default. Keep the
    # operator on traditional List+Watch because Cilium Gateway controller cache
    # sync has twice wedged after operator restarts on this cluster.
    - name: KUBE_FEATURE_WatchListClient
      value: "false"
  resources:
    requests:
      cpu: 100m
      memory: 256Mi
    limits:
      memory: 512Mi
      cpu: 1000m
  podDisruptionBudget:
    enabled: true
    maxUnavailable: 1
  topologySpreadConstraints:
    - maxSkew: 1
      topologyKey: kubernetes.io/hostname
      whenUnsatisfiable: ScheduleAnyway
      labelSelector:
        matchLabels:
          app.kubernetes.io/name: cilium-operator
          app.kubernetes.io/part-of: cilium
