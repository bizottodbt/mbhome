---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: dex
  namespace: flux-system
spec:
  interval: 30m
  releaseName: dex
  targetNamespace: dex
  chart:
    spec:
      chart: dex
      version: 0.24.1
      sourceRef:
        kind: HelmRepository
        name: dex
        namespace: flux-system
      interval: 1h
  install:
    remediation:
      retries: 3
  upgrade:
    remediation:
      retries: 3
  values:
    envFrom:
      - secretRef:
          name: dex-ldap-bind
      - secretRef:
          name: dex-postgres-app
      - secretRef:
          name: dex-grafana-client
      - secretRef:
          name: dex-vault-client
      - secretRef:
          name: dex-open-webui-client
    replicaCount: 2
    podDisruptionBudget:
      enabled: true
      maxUnavailable: 1
    topologySpreadConstraints:
      - maxSkew: 1
        topologyKey: kubernetes.io/hostname
        whenUnsatisfiable: ScheduleAnyway
        labelSelector:
          matchLabels:
            app.kubernetes.io/instance: dex
            app.kubernetes.io/name: dex
    podSecurityContext:
      runAsNonRoot: true
      runAsUser: 65532
      runAsGroup: 65532
      seccompProfile:
        type: RuntimeDefault
    securityContext:
      allowPrivilegeEscalation: false
      capabilities:
        drop:
          - ALL
      runAsNonRoot: true
    config:
      issuer: https://dex.apps.mbhome.biz
      storage:
        type: postgres
        config:
          host: dex-postgres-rw.dex.svc.cluster.local
          port: 5432
          database: dex
          user: dex
          password: "{{ .Env.password }}"
          ssl:
            mode: require
      web:
        http: 0.0.0.0:5556
      oauth2:
        skipApprovalScreen: true
      expiry:
        # ID tokens stay short-lived; kubectl oidc-login refreshes them by
        # using the offline_access refresh token cached on the client.
        signingKeys: 24h
        idTokens: 1h
        authRequests: 10m
        deviceRequests: 5m
        refreshTokens:
          # Idle lifetime: users must run kubectl at least this often to keep
          # the cached refresh session alive.
          validIfNotUsedFor: 720h
          # Maximum lifetime: users must complete a browser login again after
          # this, even if they used kubectl regularly.
          absoluteLifetime: 2160h
          reuseInterval: 3s
          disableRotation: false
      staticClients:
        - id: kubernetes
          public: true
          name: Kubernetes CLI
          redirectURIs:
            - http://localhost:8000
            - http://localhost:18000
            - http://127.0.0.1:8000
            - http://127.0.0.1:18000
        - id: grafana
          secretEnv: DEX_GRAFANA_CLIENT_SECRET
          name: Grafana
          redirectURIs:
            - https://grafana.apps.mbhome.biz/login/generic_oauth
        - id: vault
          secretEnv: DEX_VAULT_CLIENT_SECRET
          name: Vault
          redirectURIs:
            - https://vault.apps.mbhome.biz/ui/vault/auth/oidc/oidc/callback
            - http://localhost:8250/oidc/callback
        - id: open-webui
          secretEnv: DEX_OPEN_WEBUI_CLIENT_SECRET
          name: Open WebUI
          redirectURIs:
            - https://ai.apps.mbhome.biz/oauth/oidc/callback
      connectors:
        - type: ldap
          id: ad
          name: Active Directory
          config:
            host: mbhome.biz:636
            insecureNoSSL: false
            # TODO: replace with rootCAData after AD CS issues trusted DC certs.
            insecureSkipVerify: true
            bindDN: "{{ .Env.DEX_LDAP_BIND_DN }}"
            bindPW: "{{ .Env.DEX_LDAP_BIND_PASSWORD }}"
            usernamePrompt: Email Address
            userSearch:
              baseDN: OU=Users,OU=home,DC=mbhome,DC=biz
              filter: "(objectClass=person)"
              username: userPrincipalName
              idAttr: DN
              emailAttr: userPrincipalName
              nameAttr: cn
              preferredUsernameAttr: sAMAccountName
            groupSearch:
              baseDN: OU=Groups,OU=home,DC=mbhome,DC=biz
              filter: "(objectClass=group)"
              userMatchers:
                - userAttr: DN
                  groupAttr: member
              nameAttr: cn
