---
# Kubernetes CSI NFS driver values.
#
# StorageClasses are managed separately in storageclasses.yaml because
# they contain homelab-specific NFS server addresses and export paths.
controller:
  enableSnapshotter: false
  replicas: 2
  runOnControlPlane: false

storageClass:
  create: false

externalSnapshotter:
  enabled: false
